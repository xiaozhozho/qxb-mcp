[project]
name = "qx-mcp"
version = "0.1.0"
description = "启信宝 (Qixin) 数据开放平台 MCP 服务器 — 企业工商信息查询"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]

[project.scripts]
qx-mcp = "qx_mcp:run_cli"
